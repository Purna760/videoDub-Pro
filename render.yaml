services:
  - type: web
    name: videodub-pro
    runtime: docker
    dockerfilePath: ./Dockerfile
    envVars:
      - key: PORT
        value: "10000"
      - key: SECRET_KEY
        generateValue: true
    healthCheckPath: /health
    autoDeploy: true
